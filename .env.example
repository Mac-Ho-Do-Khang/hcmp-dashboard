# Leave both Turso variables unset to use a local file database, which is
# enough for development and does not need a Turso account.
TURSO_DB_URL=
TURSO_AUTH_TOKEN=

# Shared secret that must be sent in the X-Admin-Token header to upload a report.
ADMIN_TOKEN=changeme
